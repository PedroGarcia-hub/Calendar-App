import { types } from '../types/types';

// Actions to open the calendar modal
export const uiOpenModal = () => ({ type: types.uiOpenModal });

export const uiCloseModal = () => ({ type: types.uiCloseModal });
